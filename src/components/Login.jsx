import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../contexts/AuthContext';

export default function Login() {
 
  return (
     <div className="p-6">
      <h1 className="text-3xl font-bold mb-6">login halaman</h1>

    
      </div>
  );
}